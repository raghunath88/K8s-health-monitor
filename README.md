# K8s-health-monitor
A single metric travels: Worker (perf_hooks/async_hooks) → Pod master (/pod-health) → Cluster aggregator (/cluster-health) → React dashboard — all over plain HTTP, zero external tools.
